{
  "$schema": "https://static.modelcontextprotocol.io/schemas/2025-12-11/server.schema.json",
  "name": "io.github.infobloxopen/dns-aid",
  "description": "Discover and publish AI agents via DNS using SVCB records (RFC 9460)",
  "repository": {
    "url": "https://github.com/infobloxopen/dns-aid-core",
    "source": "github"
  },
  "version": "0.18.1",
  "packages": [
    {
      "registryType": "pypi",
      "identifier": "dns-aid",
      "version": "0.18.1",
      "transport": {
        "type": "stdio"
      },
      "environmentVariables": [
        {
          "description": "DNS backend to use (route53, cloudflare, infoblox, nios, ns1, ddns, mock). Not required for discovery-only operations.",
          "isRequired": false,
          "format": "string",
          "isSecret": false,
          "name": "DNS_AID_BACKEND"
        }
      ]
    }
  ]
}
